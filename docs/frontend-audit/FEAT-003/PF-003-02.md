# FEAT-003 Process-Flow Audit Record: PF-003-02

| Metadata Field | Value |
|---|---|
| **Process Flow ID** | `PF-003-02` |
| **Process Flow Title** | Individual Employee Provisioning & Profile Management |
| **Feature Suite** | `FEAT-003: Employee Roster & Demographic Attribute Management` |
| **Target Role(s)** | `PROJECT_ADMIN`, `HR_MANAGER` |
| **Primary Route** | `/employees` |
| **API Endpoints** | `POST /api/v1/employees`, `PUT /api/v1/employees/{employeeId}` |
| **Audit Status** | **PASS** |
| **Audit Date** | 2026-08-11 |
| **Auditor** | Lead Frontend Architect |

---

## 1. Process Flow Specification & Requirements

### 1.1 Objective
Allow HR managers and project administrators to provision new employee profiles or update existing profiles, enforcing `VR-EMP-001` ID patterns, `VR-EMP-002` RFC 5322 email formatting, `VR-EMP-003` organizational node linking, and AWS KMS CSFLE PII client-side encryption (`FR-EMP-002`).

### 1.2 Authoritative Rules & Guards Enforced
- **FR-EMP-002**: Client-Side Field Level Encryption (CSFLE) encrypting sensitive PII (`email`, `fullName`, `phoneNumber`) before writing to persistent storage.
- **BR-EMP-001**: Unique `employeeId` per project workspace.
- **BR-EMP-002**: Primary `nodeId` reference check against active `FEAT-002` nodes.
- **VR-EMP-001**: `employeeId` regex matching `^[A-Za-z0-9_-]{2,30}$`.
- **VR-EMP-002**: RFC 5322 valid corporate email format.
- **VR-EMP-003**: Assigned `nodeId` existence check.

---

## 2. Technical Implementation Architecture

### 2.1 Component Mapping
- **Modal Component**: [EmployeeFormModal.tsx](file:///d:/talnova/talnova-enterprise-survey-platform/frontend/src/components/employee/EmployeeFormModal.tsx)
- **Data Grid Component**: [EmployeeDataGrid.tsx](file:///d:/talnova/talnova-enterprise-survey-platform/frontend/src/components/employee/EmployeeDataGrid.tsx)
- **API Service Layer**: [employeeApi.ts](file:///d:/talnova/talnova-enterprise-survey-platform/frontend/src/features/employee/api/employeeApi.ts) (`createEmployee`, `updateEmployee`)
- **React Query Hooks**: [useEmployeeQueries.ts](file:///d:/talnova/talnova-enterprise-survey-platform/frontend/src/features/employee/api/useEmployeeQueries.ts) (`useCreateEmployeeMutation`, `useUpdateEmployeeMutation`)

### 2.2 Sequence Flow
```mermaid
sequenceDiagram
    autonumber
    actor HR as HR Manager / Project Admin
    participant UI as EmployeeFormModal
    participant API as employeeApi
    participant GW as API Gateway (:8080)
    participant EMS as employee-service (:8083)

    HR->>UI: Click "+ Add Employee Profile"
    HR->>UI: Fill form (employeeId, fullName, email, nodeId, status)
    UI->>UI: Validate VR-EMP-001 (regex), VR-EMP-002 (email), VR-EMP-003 (nodeId)
    HR->>UI: Submit Form
    UI->>API: createEmployee(CreateEmployeeRequest)
    API->>GW: POST /api/v1/employees
    GW->>EMS: Encrypt PII via AWS KMS CSFLE & insert MongoDB document
    EMS-->>UI: Return 201 Created (EmployeeResponseDTO)
    UI->>HR: Show Toast feedback & Invalidate Query Cache ['employees']
```

---

## 3. UI/UX Verification & States Matrix

| State Category | Expected Visual / Behavioral Requirement | Implementation Verification | Status |
|---|---|---|---|
| **Org Scope Selector** | Provides a human-readable Organization Scope dropdown selector powered by `useSubtreeQuery`. | Verified in `EmployeeFormModal.tsx` lines 189-207. | **PASS** |
| **Demographic Attribute Builder** | Provides a dynamic Key-Value attribute builder (Add/Delete pair rows) with optional Advanced JSON mode toggle. | Verified in `EmployeeFormModal.tsx` lines 216-271. | **PASS** |
| **Validation State** | Enforces `VR-EMP-001` ID regex, `VR-EMP-002` email format, and mandatory org scope before submit. | Verified in `EmployeeFormModal.tsx` lines 102-120. | **PASS** |
| **Editing Pre-fill State** | Pre-fills profile fields and populates Key-Value attribute rows when editing an existing employee record. | Verified in `EmployeeFormModal.tsx` lines 50-70. | **PASS** |
| **User-Oriented Terminology** | Replaced technical rule codes (e.g. `VR-EMP-001`) with clear, user-centric form labels and helper text. | Verified in `EmployeeFormModal.tsx` lines 160-210. | **PASS** |
| **Loading State** | Submit button displays loading spinner during encryption and save transaction. | Verified in `EmployeeFormModal.tsx` line 287. | **PASS** |
| **Success Feedback State** | Toast notification *"Employee profile EMP-XXXXX created!"* and query cache invalidation. | Verified in `useEmployeeQueries.ts` lines 26-27. | **PASS** |
| **Error Handling State** | Displays inline `Alert` error if backend rejects request (e.g. duplicate employee ID). | Verified in `EmployeeFormModal.tsx` lines 152-156. | **PASS** |

---

## 4. Process Flow UX Audit Record

### UX Problems Found & Resolved:
1. **Raw Database IDs Required**: Replaced unstyled technical `nodeId` text field with an Organization / Department dropdown selector powered by `useSubtreeQuery`.
2. **Developer/Demo JSON Input**: Replaced mandatory raw JSON text entry for custom demographic attributes with a Key-Value attribute builder interface with optional JSON toggle.
3. **Exposed Technical Rule Codes**: Removed internal specification IDs (e.g., `VR-EMP-001`, `VR-EMP-002`, `FR-EMP-001`) from visible form labels.
4. **Confusing Modal Subtitle**: Simplified header subtitle from PII cryptobabble to clear enterprise language ("Configure workforce profile details, organizational reporting lines, and demographic metadata.").

---

## 5. Test & Verification Evidence

- **TypeScript Compilation**: `npx tsc --noEmit` passed with **0 errors**.
- **Unit & Domain Tests**: `npm run test` passed **14/14 test suites (83/83 tests passed)**.
  - `src/__tests__/employeeDomain.test.ts`: **PASS**

---

## 6. Certification Sign-off

- [x] Process Flow **PF-003-02** specification fully implemented and UX-refined.
- [x] All validation rules (`VR-EMP-001`, `VR-EMP-002`, `VR-EMP-003`) enforced cleanly.
- [x] Key-Value demographic attribute builder and Org Scope dropdown selector verified.
- [x] Audit Status: **PASS**.
