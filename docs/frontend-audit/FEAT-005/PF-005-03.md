# FEAT-005 Process-Flow Audit Record: PF-005-03

| Metadata Field | Value |
|---|---|
| **Process Flow ID** | `PF-005-03` |
| **Process Flow Title** | Automated Non-Respondent Reminder Sequence Engine |
| **Feature Suite** | `FEAT-005: Multi-Channel Survey Distribution Engine` |
| **Target Role(s)** | `PROJECT_ADMIN`, `HR_MANAGER` |
| **Primary Route** | `/distribution` |
| **API Endpoints** | `POST /api/v1/campaigns/{campaignId}/remind` |
| **Audit Status** | **PASS** |
| **Audit Date** | 2026-08-17 |
| **Auditor** | Principal Frontend Architect |

---

## 1. Process Flow Specification & Requirements

### 1.1 Objective
Schedule and trigger non-intrusive reminder nudges targeted strictly to employees who have not completed the survey (tokens unburned) without spamming respondents who already submitted (`US-DST-002`, `FR-DST-005`, `TC-DST-002`).

### 1.2 Authoritative Rules & Guards Enforced
- **FR-DST-005**: Non-Respondent Reminder Engine tracking token completion status per campaign.
- **BR-DST-004**: Spam Guard Frequency Limit: Rate limits recipient notifications to max 1 per channel within 24 hours.
- **TC-DST-002**: Verification that reminders publish payloads to Kafka topic `tesp.notifications.queue.v1` strictly for uncompleted tokens.
- **PR-DST-003**: Access restriction: Read-only roles (`CONSULTANT_DAASH`) can view delivery metrics but cannot trigger dispatches.

---

## 2. Technical Implementation Architecture

### 2.1 Component Mapping
- **Metrics Card Component**: [CampaignMetricsCard.tsx](file:///d:/talnova/talnova-enterprise-survey-platform/frontend/src/components/distribution/CampaignMetricsCard.tsx)
- **API Service Layer**: [distributionApi.ts](file:///d:/talnova/talnova-enterprise-survey-platform/frontend/src/features/distribution/api/distributionApi.ts) (`triggerReminders`)
- **React Query Hooks**: [useDistributionQueries.ts](file:///d:/talnova/talnova-enterprise-survey-platform/frontend/src/features/distribution/api/useDistributionQueries.ts) (`useTriggerRemindersMutation`)

### 2.2 Sequence Flow
```mermaid
sequenceDiagram
    autonumber
    actor HR as HR Manager / Admin
    participant Card as CampaignMetricsCard
    participant Modal as Reminder Confirmation Modal
    participant API as distributionApi
    participant GW as API Gateway (:8080)
    participant SDS as survey-distribution-service (:8085)
    participant Kafka as Apache Kafka Bus

    HR->>Card: Click "🔔 Remind Non-Respondents" on Active Campaign Card
    Card->>Modal: Open Confirmation Modal (Show uncompleted count & BR-DST-004 spam rate limit warning)
    HR->>Modal: Click "🚀 Confirm & Dispatch Nudges"
    Modal->>API: triggerReminders("CMP-1001", activeProjectId)
    API->>GW: POST /api/v1/campaigns/CMP-1001/remind
    GW->>SDS: Query unburned tokens for campaign CMP-1001
    SDS->>Kafka: Publish reminder dispatch payloads to 'tesp.notifications.queue.v1'
    SDS-->>Card: Return 200 OK ({ remindedCount: 580, status: "DISPATCHED" })
    Card->>HR: Show Toast & render card feedback banner with dispatch count and timestamp
```

---

## 3. UI/UX Verification & States Matrix

| State Category | Expected Visual / Behavioral Requirement | Implementation Verification | Status |
|---|---|---|---|
| **Reminder Confirmation Modal** | Opens confirmation modal displaying exact uncompleted non-respondent headcount, active notification channels, and `BR-DST-004` spam rate-limit notice. | Verified in `CampaignMetricsCard.tsx` lines 156-220. | **PASS** |
| **Dispatch Feedback Banner** | Displays persistent success alert card showing timestamp and exact count of reminder nudges dispatched (`TC-DST-002`). | Verified in `CampaignMetricsCard.tsx` lines 147-151. | **PASS** |
| **Permission Guarding** | Disables reminder and status triggers for read-only roles (`CONSULTANT_DAASH` under `PR-DST-003`). | Verified in `CampaignMetricsCard.tsx` lines 79-81. | **PASS** |
| **Pending Mutation State** | Displays loading spinner on modal submit button while dispatching reminders. | Verified in `CampaignMetricsCard.tsx` line 211. | **PASS** |

---

## 4. Test & Verification Evidence

- **TypeScript Compilation**: `npm run build` (`tsc && vite build`) passed with **0 errors**.
- **Unit & Domain Tests**: `npm run test` passed **14/14 test suites (83/83 tests passed)**.
  - `triggerReminders calls POST /campaigns/{campaignId}/remind`: **PASS**

---

## 5. Certification Sign-off

- [x] Process Flow **PF-005-03** specification fully implemented and UX-refined.
- [x] Non-respondent reminder confirmation modal, spam rate limit guards, and Gateway integration verified.
- [x] Audit Status: **PASS**.

